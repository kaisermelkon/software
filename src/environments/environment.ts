// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

/**
   * @ignore
 */
export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyAdK1fZcF1Li9wKorDRIESPvceS4RL8q9k",
    authDomain: "metrouber-2be7c.firebaseapp.com",
    databaseURL: "https://metrouber-2be7c.firebaseio.com",
    projectId: "metrouber-2be7c",
    storageBucket: "metrouber-2be7c.appspot.com",
    messagingSenderId: "580036238807",
    appId: "1:580036238807:web:ef4bf9e613784449"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
